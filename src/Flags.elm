module Flags exposing (Flags)


type alias Flags =
    { radius : Float
    , showStella : Bool
    , levelsCleared : Int
    , timestamp : Int

    --, lastLevelProgress :
    --    Maybe
    --        { nodes : Array Node
    --        , edges : List Edge
    --        }
    }
